# These are defaults — actual values are normally set via the Credentials page in the UI,
# which persists them to output/web_config.json and injects them into process.env at runtime.
# Use this file only if you want to seed values before first run.

NIMBLE_API_KEY=492ab4da4c984e478cd365d16de55e4dddd110df39984883abf890abc82ef7f8
NIMBLE_API_URL=https://api.webit.live/api/v1/realtime/web

TARGET_API_URL=
TARGET_API_KEY=

FB_EMAIL=
FB_PASSWORD=

STARBUYERS_EMAIL=
STARBUYERS_PASSWORD=

REQUEST_TIMEOUT_MS=30000
NUXT_PORT=3000
