{
  "name": "poc-pricetrends",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "preview": "pnpm -r --parallel preview",
    "dev:api": "pnpm --filter poc-pricetrends-api dev",
    "dev:web": "pnpm --filter poc-pricetrends-web dev",
    "build:pages": "pnpm --filter poc-pricetrends-web build:pages",
    "pages:deploy": "pnpm --filter poc-pricetrends-web pages:deploy"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
